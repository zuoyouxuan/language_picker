name: analyzer
on: [push]
jobs:
  run-analyzer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v1
      - run: flutter pub get
      - run: flutter pub pub run language_picker:build_languages
      - run: flutter analyze